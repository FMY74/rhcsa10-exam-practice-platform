// Command reference map (command name -> reference entry).
// Left empty in the public demo; the real reference is generated from the
// (excluded) study notes. The UI treats an empty map as "no inline reference".
const CMD_REF = {};
if (typeof module !== 'undefined') { module.exports = CMD_REF; }
